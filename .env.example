# Reference only. Runtime files belong in server/.env and client/.env.
# No .env file or credentials are required to run Phase 1 locally.
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173
DATABASE_URL=
DATABASE_URL_UNPOOLED=
JWT_SECRET=
JWT_EXPIRES_IN=7d
VITE_API_BASE_URL=http://localhost:5000/api/v1
